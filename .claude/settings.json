{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE '\\-\\-target\\s+prod'; then echo 'ERROR: Direct prod deploys are blocked. Merge to main to deploy via CI/CD.' >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
